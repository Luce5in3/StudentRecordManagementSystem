<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.student.archive.mapper.SysOperationLogMapper">

    <resultMap id="BaseResultMap" type="com.student.archive.entity.SysOperationLog">
        <id column="pk_log" property="pkLog"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="operation" property="operation"/>
        <result column="module" property="module"/>
        <result column="description" property="description"/>
        <result column="request_params" property="requestParams"/>
        <result column="ip" property="ip"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM sys_operation_log
        <where>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="module != null and module != ''">
                AND module = #{module}
            </if>
            <if test="operation != null and operation != ''">
                AND operation = #{operation}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="pkLog">
        INSERT INTO sys_operation_log (user_id, operation, module, description, request_params, ip)
        VALUES (#{userId}, #{operation}, #{module}, #{description}, #{requestParams}, #{ip})
    </insert>

    <select id="selectRecent" resultMap="BaseResultMap">
        SELECT l.*, u.real_name AS user_name
        FROM sys_operation_log l
        LEFT JOIN sys_user u ON l.user_id = u.pk_user
        ORDER BY l.create_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT l.*, u.real_name AS user_name
        FROM sys_operation_log l
        LEFT JOIN sys_user u ON l.user_id = u.pk_user
        <where>
            <if test="userId != null">
                AND l.user_id = #{userId}
            </if>
            <if test="module != null and module != ''">
                AND l.module LIKE CONCAT('%', #{module}, '%')
            </if>
            <if test="operation != null and operation != ''">
                AND l.operation LIKE CONCAT('%', #{operation}, '%')
            </if>
        </where>
        ORDER BY l.create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countPage" resultType="int">
        SELECT COUNT(*) FROM sys_operation_log l
        <where>
            <if test="userId != null">
                AND l.user_id = #{userId}
            </if>
            <if test="module != null and module != ''">
                AND l.module LIKE CONCAT('%', #{module}, '%')
            </if>
            <if test="operation != null and operation != ''">
                AND l.operation LIKE CONCAT('%', #{operation}, '%')
            </if>
        </where>
    </select>
</mapper>
